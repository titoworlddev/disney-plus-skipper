import { AppLanguage } from './language.types';

export const enGBLanguage: AppLanguage = {
  introCheckboxLabel: 'Skip intro',
  jumpCheckboxLabel: 'Jump to next episode',
  resumeCheckboxLabel: 'Skip recap',
  skipperSwitchLabel: 'Activated'
};
